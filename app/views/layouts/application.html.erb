<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Credly Take-home</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_pack_tag "application" %>
    <%= stylesheet_pack_tag "application" %>
  </head>

  <body>
<style media="screen">
  body {
    background-color: #DCDCDC;
  }

</style>
    <div id="flashMessage">
      <% flash.each do |type, message| %>
        <h1><%= message %></h1>
      <% end %>
    </div>

    <!-- load React header component instead -->
    <%#= javascript_pack_tag "credly_take_home_header" %>

    <%= yield %>
  </body>
</html>
